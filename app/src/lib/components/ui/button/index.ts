import Button from "./button.svelte";
import {
	type ButtonProps,
	type ButtonVariant,
	type ButtonSize,
	buttonVariants,
} from "./button.svelte";

export {
	Button,
	type ButtonProps,
	type ButtonVariant,
	type ButtonSize,
	buttonVariants,
};
export default Button;
